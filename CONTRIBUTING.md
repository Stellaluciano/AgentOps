# Contributing

Thanks for your interest in AgentOps.

## Development setup

1. Start infra with `docker compose up --build`.
2. Create a Python 3.11 virtualenv.
3. Install dependencies with `pip install -e .`.
4. Run tests with `pytest`.

## Pull requests

- Keep PRs focused.
- Add tests for behavior changes.
- Update docs when API or UX changes.
